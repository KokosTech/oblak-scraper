export type Image = {
    id: number;
    url: string;
    alt: string;
};

export type ImageRequest = {
    url: string;
    alt: string;
};

export type ImageResponse = {
    url: string;
    alt: string;
};